apiVersion: 1
datasources:
  - name: ServiceTree CSV Demo
    type: marcusolsson-csv-datasource
    access: proxy
    uid: csv-demo
    isDefault: true
    editable: true
    jsonData:
      storage: local

  # ---------------------------------------------------------------------------
  # Optional: connect to a real Zabbix server. Uncomment, set ZABBIX_URL and
  # credentials, then restart the stack. The Service Tree panel can then use
  # the "Zabbix" field preset to consume the Services query mode directly.
  # ---------------------------------------------------------------------------
  # - name: Zabbix
  #   type: alexanderzobnin-zabbix-datasource
  #   access: proxy
  #   uid: zabbix-main
  #   url: http://zabbix-web:8080/api_jsonrpc.php
  #   editable: true
  #   jsonData:
  #     username: Admin
  #     trends: true
  #     trendsFrom: 7d
  #     trendsRange: 4d
  #     cacheTTL: 1h
  #     disableReadOnlyUsersAck: false
  #   secureJsonData:
  #     password: zabbix
